import Link from "next/link";
import { ArrowRight, CalendarDays, MapPin, Ticket } from "lucide-react";
import { DashboardShell } from "@/components/DashboardShell";
import { events } from "@/lib/data";
export default function UserDashboard(){return <DashboardShell role="user"><main className="dash-content"><div className="dash-title"><div><span className="eyebrow">YOUR SPACE</span><h1>Hello, Farha.</h1><p>Here’s what’s waiting for you.</p></div><Link href="/events" className="button button-dark">Discover events <ArrowRight size={17}/></Link></div><section className="next-ticket"><div className={`ticket-art ${events[1].color}`}><span>{events[1].icon}</span><i>UP NEXT</i></div><div className="ticket-info"><span className="eyebrow">YOUR NEXT EVENT</span><h2>{events[1].title}</h2><p><CalendarDays size={16}/>{events[1].date} · {events[1].time}</p><p><MapPin size={16}/>{events[1].location}</p><Link href="/user/tickets">View ticket <Ticket size={16}/></Link></div><div className="qr-placeholder">▦<small>SCAN AT ENTRY</small></div></section><div className="dash-section-head"><div><h2>Recommended for you</h2><p>Based on what you love.</p></div><Link href="/events">See all <ArrowRight size={16}/></Link></div><div className="mini-events">{events.slice(2,5).map(e=><Link href={`/events/${e.slug}`} key={e.slug}><span className={`mini-art ${e.color}`}>{e.icon}</span><div><small>{e.date} · {e.category}</small><b>{e.title}</b><span>{e.location}</span></div></Link>)}</div></main></DashboardShell>}
